package World.TerrainGen;

public class TerrainGen{
    /*~~~~~~~~~~~~~~~~~~~~~~~~~~~
     *  TerrainGen
     *~~~~~~~~~~~~~~~~~~~~~~~~~~~~
     *  Generate terrain
     */
    public void generate(int x, int y, int z){
        //I can generate a block of terra
        //in based off where the camera is, If the cords are out
        //of range of the octree expand in that direction and add the offset?

        //Identifier where the camera is, if the octree location does not exist,
        //generate it and offset origin

        //Increase depth and gen a new octree if necessary

        //set all leaves above octree depth 1 as empty
        //all octrees that are generated bellow 4 at depth one relative to the root


        //all leaves below stone
    }

}
